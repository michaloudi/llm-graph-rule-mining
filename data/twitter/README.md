# Twitter Graph Dataset

This directory contains the schema used for the Twitter property graph experiments.

The graph includes entities such as users, tweets, hashtags, links, and sources, together with relationships representing interactions including following, posting, mentioning, retweeting, replying, and tagging.

The full graph export is not included in this repository.
